trigger: none

pool:
  vmImage: 'ubuntu-latest' 

variables:
  major: 0
  minor: 0

stages:
  - stage: Build_Client
    displayName: "Build client"
    dependsOn: []
    jobs:
      - template: 'build-jobs.yml'
        parameters:
          package: client
          isPr: true